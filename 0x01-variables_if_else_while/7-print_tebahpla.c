#include <stdio.h>
/**
 *main - Prints the lowercase alphabet in reverse.
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	char 1;
	for (1 = 'z'; 1 >= 'a'; l--)
	putchar(1);
	putchar('\n');
	return (0);
}
